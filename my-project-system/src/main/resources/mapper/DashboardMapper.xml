<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.my_project_system.mapper.DashboardMapper">

    <select id="countTodayOrder" resultType="int">
        SELECT COUNT(*)
        FROM erp_order
        WHERE DATE(create_time) = CURDATE()
    </select>

    <select id="sumMonthSales" resultType="java.math.BigDecimal">
        SELECT IFNULL(SUM(total_amount), 0)
        FROM erp_order
        WHERE MONTH(create_time) = MONTH(NOW())
    </select>

    <!-- ✅ 这里必须转义 -->
    <select id="countStockWarning" resultType="int">
        SELECT COUNT(*)
        FROM erp_product
        WHERE stock_count &lt; warn_count
    </select>

    <select id="countTodo" resultType="int">
        SELECT COUNT(*)
        FROM erp_todo
        WHERE status = 0
    </select>

    <!-- ✅ 这里必须转义 -->
    <select id="countOnlineUser" resultType="int">
        SELECT COUNT(*)
        FROM sys_user
        WHERE last_login_time &gt;= NOW() - INTERVAL 30 MINUTE
    </select>

</mapper>